/** Browser/document title for public academy pages — academy name leads. */
export function publicAcademyDocumentTitle(
  clubName: string,
  pageTitle: string,
): string {
  return `${clubName} | ${pageTitle}`;
}

export const POWERED_BY_DOJO_DIRECTOR_LABEL = "Powered by Dojo Director";
